"""This package defines and implements event streams backend.

An event stream backend is a class which is able to receive arbitrary
metadata from a deepstream buffer probe and quickly post it in an
external service.

"""
